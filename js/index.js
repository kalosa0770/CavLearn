// Menu Mobile Bar
function menuBar() {
    const menubar = document.querySelector(".sideBar")
    menubar.style.display = 'flex'
}

// Close Menu Bar
function closeBar() {
    const menubar = document.querySelector(".sideBar")
    menubar.style.display = 'none'
}